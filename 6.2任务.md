为了让你在开工前有最直观的视觉感知，我先为你绘制出**放大模式（3天一屏）\**与\**缩小模式（7天一屏）\**的\**“像素级手机端视觉大图（ASCII Wireframe）”**，紧接着送上为 Claude Code 重新封装、无懈可击的**终极全量施工指令集**！

## 🎨 核心排期页：全新“剧场流（Theater Flow）”视觉大图

### 🔍 视图一：【放大聚焦模式】（一屏显示 3 天体量）

- **视觉核心**：海报即卡片，横向撞档平铺两张，多余向右丝滑滚动，文字完美悬浮。

Plaintext

```
+-------------------------------------------------------------+
|  2026年6月                                           [ + ]  | <-- 顶部操作栏
+-------------------------------------------------------------+
| [06-02 今天] ────────────────────────────────────────────── | <-- Sticky 吸顶日期线
|   +-----------------------+   +-----------------------+     |
|   | 🕒 19:30              |   | 🕒 19:30              |     | <-- 开场时间悬浮胶囊
|   |                       |   |                       |     |
|   |                       |   |                       |     |
|   |     ( 剧目海报 A )     |   |     ( 剧目海报 B )     | ──▶ | <-- 45%屏宽, 横向排2张
|   |                       |   |                       |     |     第3张露出边缘可右滑
|   |                       |   |                       |     |
|   |=======================|   |=======================|     |
|   | 《悲惨世界》          |   | 《罗密欧与朱丽叶》    |     | <-- 底部全暗渐变蒙层
|   | 上海大剧院 · 阿云嘎   |   | 文化广场 · 演员名...  |     |     单行截断微型字
|   +-----------------------+   +-----------------------+     |
+-------------------------------------------------------------+
| [06-03 明天] ────────────────────────────────────────────── | <-- 往上滑时，自然把今天顶走
|                                                             |
|   ( 干净的留白空间，透出淡淡的底色与 0.5 像素的暗灰色表格线 )  | <-- 留白日的呼吸感
|                                                             |
+-------------------------------------------------------------+
| [06-04 周四] ────────────────────────────────────────────── |
|   +-----------------------+                                 |
|   | 🕒 14:00              |                                 |
|   |     ( 剧目海报 C )     |                                 | <-- 单场演出同样大小
|   |=======================|                                 |
|   | 《大卫科波菲尔》      |                                 |
|   +-----------------------+                                 |
+-------------------------------------------------------------+
```

### 🔍 视图二：【缩小微观模式】（双指捏合，一屏平分 7 天体量）

- **视觉核心**：极致紧凑的“海报邮票墙”，时间直接戳在海报脸上，纵向高度完美平分。

Plaintext

```
+-------------------------------------------------------------+
|  2026年6月 (第23周宏观视图)                           [ + ]  |
+-------------------------------------------------------------+
| [06-01 周一] ── 浅浅的 0.5px 表格线下底线 ─────────────────── | <-- 纵向高度均分(约90px)
+-------------------------------------------------------------+
| [06-02 周二] ──   +---------+   +---------+                 |
|                   |  14:00  |   |  19:30  |                 | <-- 极致裁切的3:4微海报
|                   | (海报A) |   | (海报B) |                 |     时间戳直接加粗居中
|                   +---------+   +---------+                 |     悬浮在海报正中央
+-------------------------------------------------------------+
| [06-03 周三] ────────────────────────────────────────────── | <-- 空白日子无海报，仅保留
+-------------------------------------------------------------+     淡淡的表格细线
| [06-04 周四] ──   +---------+                               |
|                   |  19:30  |                               |
|                   | (海报C) |                               |
|                   +---------+                               |
+-------------------------------------------------------------+
| [06-05 周五] ────────────────────────────────────────────── |
+-------------------------------------------------------------+
| [06-06 周六] ──   +---------+   +---------+   +---------+   |
|                   |  14:00  |   |  19:30  |   |  21:45  |   | <-- 撞档期横向排开
|                   | (海报D) |   | (海报E) |   | (海报F) |   |     特种兵刷剧一目了然
|                   +---------+   +---------+   +---------+   |
+-------------------------------------------------------------+
| [06-07 周日] ────────────────────────────────────────────── |
+-------------------------------------------------------------+
```

## 🚀 交付给 Claude Code 的最终全量施工指令集

你可以直接**分步**或者**整体**将以下精心打磨的指令喂给 Claude Code：

### 📌 任务一：数据库 Model 与 Schema 升级（准备工作）

Plaintext

```
请帮我升级数据库中剧目（Show）的数据模型和 SQLite Schema，以支持剧目封面海报功能：
1. 找到剧目的 Model 文件（可能是 show.dart 或相关的 model 文件）以及 DatabaseHelper 里的建表语句。
2. 在 `shows` 表和 Model 中新增一个字段：`cover_path`（类型为 TEXT，可为空 Nullable），用于存储用户上传的本地封面图片路径。
3. 更新 Model 的 `toMap()` 和 `fromMap()` 方法。
4. 在 DatabaseHelper 的数据库初始化或版本升级逻辑中，确保新增该列（如果是已有数据库，请使用 ALTER TABLE 语句进行迁移防御，防止本地数据丢失）。
```

### 📌 任务二：重构输入与编辑一体化页面（`AddShowScreen`）

Plaintext

```
请重构输入与编辑界面 `AddShowScreen`，使其完美支持“手动录入”、“OCR识别后带入”以及“二次修改编辑”三种模式，并升级底层存储逻辑与视觉美化：

1. 构造函数改造：
   - 接收参数：`final Show? initialShow;`（初始剧目）、`final List<Performance>? initialPerformances;`（初始场次列表）、`final bool isEditMode;`（默认 false）。在 `initState` 中据此初始化表单。

2. 头部视觉重构（彻底移除原有的横向并排）：
   - 左侧：新增一个固定 3:4 比例的精致海报封面位（宽约 90-100px）。点击后调用 `image_picker` 允许用户选择相册或拍照。
   - 封面缺省状态：读取剧目主题色（或通过 show.id % 8 映射的暗黑 8 色卡），生成纯色背景的 3:4 矩形，中心用大字显示剧名前两个字。
   - 右侧：将“剧目名称”和“演出地点”的输入框改为【纵向上下叠放】排列，拉满剩余宽度。

3. 表单表格交互与时间滚动弹窗升级：
   - 将场次卡司列表的表头修改为深色的 `Surface-2` 工业级色块（如 #1E1E1E 或 #2A2A2A），符合 Spotify 暗黑沉浸风格。
   - 彻底移除此页面内部原有的“📸 图片识别卡司”大按钮。
   - 核心交互改动：当用户点击表格中的“日期”或“时间”单元格时，不要弹出系统原生反差过大的日历，而是弹出一个位于屏幕下方的暗黑风格 showModalBottomSheet。内部嵌入 `CupertinoDatePicker`（或 CupertinoTimerPicker），以【滚轮形式】供用户丝滑滚动选择年月日和时间，点击右上角确定后回填表格。

4. 场次编辑与事务级保存交互：
   - 每一行场次右侧保留 `(➖)` 减号按钮。点击时仅在内存列表中移除该行并 `setState`，不立刻触发删除。
   - 当用户点击右上角“保存”时，必须使用 SQLite Transaction 事务：先根据 show_id 清空该剧目原本在 performances 表里的所有旧场次，再将当前表单内存里的最新场次数据批量 Bulk Insert 写入，杜绝冗余脏数据。保存成功后执行 Navigator.pop(context, true)。
```

### 📌 任务三：彻底重构排期页为全新的“剧场流（Theater Flow）时间轴”

Plaintext

```
请彻底重构现有的排期页/甘特图页（`gantt_screen.dart` 或 `calendar_screen.dart`），打破原有的左剧目右时间的表格结构，改为完全流式的、支持双指缩放的纵向海报时间轴：

1. 顶部栏布局溢出修复：
   - 修复右上角 `+` 号按钮附近的 `OVERFLOWED BY...` 错误。请检查顶部 `Row` 组件，使用 `Expanded` 包裹日期区间或调整间距/Padding，确保在任何屏幕下都不右侧溢出。

2. 全新“剧场流时间轴”状态设计：
   - 引入一个状态变量或 Enum（如 `TimelineMode { focus3Day, micro7Day }`），并使用 `GestureDetector` 的 `onScaleUpdate` 手势来监听用户的双指张开与捏合（Pinch to Zoom）。当手势跨越阈值时，实现离散弹性切换。
   - 无论处于哪种模式，所有日子的上下边缘都拉出一条极细（0.5 像素）、颜色极其克制的深灰暗色表格边线（如 `Colors.white.withOpacity(0.06)` 或 `#262626`）。没有演出的日子无需刻意写“无观剧”，仅保留吸顶日期标签和整齐的浅浅表格线，营造留白呼吸感。
   - 日期联动：左侧的日期标签必须具备 Sticky Header（吸顶效果）。当向上滑动列表时，明天的日期标签自然地将今天的标签顶上去，左上角动态更新。

3. 模式 A：【放大聚焦模式】（Focus 3-Day View）
   - 页面纵向等分渲染 3 天的体量。
   - 当天若有多场戏（撞档期），横向单行并排排列剧目卡片，每个卡片固定占用【屏幕总宽度的 45%】。如果超过2场，右侧第3场自然露出边缘（Peek 效果），支持横向微滑。
   - 卡片内部像素级设计：直接以整张【3:4 剧目海报】作为卡片背景。海报左上角悬浮一个半透明暗色胶囊包裹的醒目时间（如 🕒 19:30）。海报底部 30% 区域做一个由下往上的黑色渐变蒙层（Gradient Mask），上面用白色/浅灰色叠放单行截断的《剧目名称》，以及极小字号的剧场名/卡司。

4. 模式 B：【缩小微观模式】（Micro 7-Day View）
   - 页面纵向完美均分渲染 7 天（一整周）的物理体量。
   - 此时为了防止文字截断，自动【隐藏剧目文字名称和下方的演员卡司】。
   - 每一个场次卡片变成高度约 90px 的微型 3:4 海报缩略图。
   - 开场时间（如 14:00 / 19:30）直接以醒目的微型加粗字体，【居中悬浮叠加在每张微型海报正中央】，全屏看过去呈现“海报邮票墙”的震撼宏观排期感。当天撞多场戏时，微海报在行内整齐横向排开。

5. `+` 号按钮增加入口分流弹窗与错误处理：
   - 点击顶部 `+` 号时，弹出一个暗黑风格的 `showModalBottomSheet`：
     - 选项一：📝 “手动录入新排期”。点击跳转至空白的 `AddShowScreen(isEditMode: false)`。
     - 选项二：📸 “拍照/相册识别卡司”。点击立刻唤起 `image_picker`。
   - OCR 错误留在原地逻辑：选图后静默调用 `recognizeTextAuto()`：
     - 【解析成功】：将提取到的剧目名、时间、卡司作为参数，传入并打开 `AddShowScreen(...)` 让用户直接进入校对状态。
     - 【解析失败】：【不要跳转页面】，继续停留在当前的排期大首页，并弹出一个标准的 `AlertDialog` 弹窗提示用户：“识别失败，请上传更清晰的排期表”。让用户决定是重新找张图识别，还是再次点击手动录入。

6. 左侧剧目栏与卡片点击联动：
   - 当用户在时间轴上点击任何一个剧目卡片（大模式的海报或小模式的微缩海报）时，查询该剧目的完整数据，跳转打开 `AddShowScreen(initialShow: currentShow, initialPerformances: list, isEditMode: true)` 进入二次编辑流程。
   - 页面返回刷新：主页面跳转 `AddShowScreen` 必须使用 `await`。当编辑页保存退出返回 `true` 时，排期页立刻触发首页数据重新载入（如 `_loadData()`）并 `setState` 局部重绘。
```

### ⚠️ 【底层工程实现硬性要求】（随任务一起发给 Claude）

Plaintext

```
请在实现上述功能时，严格遵守以下底层文件系统安全规范：

1. 图片持久化命名防翻车（考虑同名及改名 Case）：
   - 禁止直接将 image_picker 返回的临时 Cache 路径存入数据库。必须引入 `path_provider` 插件获取 `getApplicationDocumentsDirectory()`，在其中创建 `covers/` 目录。
   - 命名与覆盖防御：为了防止多年后看同名复排剧目导致文件覆盖冲突，命名规则必须为：“剧目名称_当前时间戳_封面.jpg”（例如：罗密欧与朱丽叶_1717328400_封面.jpg）。
   - 安全字符过滤：在重命名和复制文件前，必须对“剧目名称”进行字符串过滤，移除或替换掉操作系统的非法文件名字符（如 \ / : * ? " < > | 等），防止保存时报错。
   - 改名联动防御（二次编辑）：在编辑模式下，如果用户修改了“剧目名称”但【没有更换海报图片】，请检查底层文件。为了保持文件系统的洁癖与一致性，请自动将本地旧文件重命名为契合新剧名的“新剧目名称_原时间戳_封面.jpg”，并同步更新数据库中的 `cover_path` 路径。最后将该永久绝对路径持久化存入 shows 表的 cover_path 字段中。
```

这套方案和指令可以说是移动端日程管理应用里天花板级别的设计了。现在就把它丢给 Claude Code 吧，看它如何用 Flutter 魔法把你的“戏梦人生时间轴”完美具象化！期待你的 Phase 1 完美收网！🚀🎬